// Re-exportado de @inventech/shared-types — edite lá, não aqui
export type {
  UserRole,
  UserRole as Role,
  AuthUser,
  LoginResponse,
  AuthMeResponse,
} from '@inventech/shared-types';

export {
  ROLE_HIERARCHY,
  ROLE_LABELS,
  hasRole,
} from '@inventech/shared-types';
